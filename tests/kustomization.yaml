apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
secretGenerator:
  - name: test-secret
    files:
      - key=/home/joelee/github/skustomize/tests/age/key.txt
    literals:
      - ref+sops://secrets.yaml
      - publicKey=ref+sops:///home/joelee/github/skustomize/tests/secrets.yaml#/publicKey
